package Data;

public class ParkingSpot {
    public int spotNumber;
    public boolean is_available;

    public ParkingSpot(int spotNumber, boolean is_available) {
        this.spotNumber = spotNumber;
        this.is_available = is_available;
    }
}
