package org.example;

public class CurrentUser {
    /**
     * Static variable to hold the Member ID (M_id) of the currently logged-in user.
     * This will be set by LoginPage upon successful login.
     */
    public static int M_id;
}